<?xml version="1.0" encoding="UTF-8"?>
<migration>
    
    <migrator name="ElInWarningTest">
        
        <action type="manual" var="action">
            <!--
            <warning>workdir=${workdir}</warning>
            <warning>srcServer.dir=${srcServer.dir}</warning>
            <warning>destServer.dir=${destServer.dir}</warning>
            <warning>action.class.name=${action.class.name}</warning>
            <warning>userVarTest=${userVarTest}</warning>
            -->
            <warning>
                workdir=${workdir.absolutePath} 
                srcServer.dir=${srcServer.dir}
                destServer.dir=${destServer.dir}
                action.class.name=${action.class.name}
                userVarTest=${userVarTest}
            </warning>
        </action>
        
    </migrator>
    
</migration>
